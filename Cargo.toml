[package]
name = "svm-from-scratch"
version = "0.1.0"
edition = "2024"

[dependencies]
plotters = { version = "0.3", default-features = false, features = ["svg_backend", "line_series", "ttf", "bitmap_backend", "bitmap_encoder"] }
rand = "0.10.1"
